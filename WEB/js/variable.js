const base_url = "http://localhost:8080";
const errorLengthProject = "Minimum length name project - 5 characters";
const errorLengthTask = "Minimum length title task - 5 characters";
const modalAuth = new bootstrap.Modal(document.querySelector('#modal-auth'));
const modalRegister = new bootstrap.Modal(document.querySelector('#modal-register'));